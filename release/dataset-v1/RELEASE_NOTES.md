# ELFNet A/AB Sweep Dataset v1

Triplet archive:

```text
<stem>_sad.npy
<stem>_elf.npy
<stem>_sym.npy
```

Summary:

```text
complete triplets: 77,279
unpacked size: about 21 GB
elemental A examples: 2,158
binary AB examples: 75,121
unique grid shapes: 38
```

Fetch with `git lfs pull --include="release/dataset-v1/**"`. Verify with
`SHA256SUMS` and `SHA256SUMS.full`.
