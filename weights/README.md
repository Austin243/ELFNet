# Weights

Place the best ChiNet checkpoint here as:

```text
weights/best_chinet_epoch0114.ckpt
```

The local source checkpoint on this machine is:

```text
/home/aellis/ChiNet/checkpoints_sad2elf/batch2/SAD2ELF_20251104_124933/best_epoch=0114.ckpt
```

The checkpoint is about 334 MB, so it is ignored by default and should be
published with Git LFS, a GitHub Release asset, or an external model host.
